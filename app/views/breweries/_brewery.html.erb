<div id="<%= dom_id brewery %>">
  <p>
    <h2>
    <%= link_to brewery.name, brewery %>
    </h2>
  </p>

  <p>
    <i>Established in
    <%= brewery.year %>
    </i>
  </p>
  <p>
    Beers count:
    <%= brewery.beers.count %>
  <p>
    <% brewery.beers.each do |beer| %>
      <%= link_to beer.name, beer %>
    <% end %>
  </p>

</div>

